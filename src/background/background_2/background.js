import React from "react"
import "./style.css"

const IllustrationBackgroundAlt = ({text, href, children}) => (
    <div className={"illustration-bg_2"}>
        {children}
    </div>
);

export default IllustrationBackgroundAlt
