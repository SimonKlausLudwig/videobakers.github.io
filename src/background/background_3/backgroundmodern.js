import React from "react"
import "./style.css"

const IllustrationBackgroundModern = ({text, href, children}) => (
    <div className={"illustration-bg_3"}>
        {children}
    </div>
);

export default IllustrationBackgroundModern
