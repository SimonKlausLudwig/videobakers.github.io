import PropTypes from "prop-types"
import React from "react"
import style from "./style.css";

const Principles = ({children}) => {
    return <div className={"shiftgrid"}>
        {children}
    </div>
};


export default ShiftGrid
